# report error if negative value win_size or num_win arguments supplied

    Code
      bed_makewindows(x, num_win = -1)
    Condition
      Error in `bed_makewindows()`:
      ! `win_size` and `num_win` must be >= 0

---

    Code
      bed_makewindows(x, win_size = -1)
    Condition
      Error in `bed_makewindows()`:
      ! `win_size` and `num_win` must be >= 0

