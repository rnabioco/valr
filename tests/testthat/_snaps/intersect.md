# incorrect `suffix` args throw errors

    Code
      bed_intersect(x, y, suffix = "TESTING", min_overlap = 0L)
    Condition
      Error in `check_suffix()`:
      ! `suffix` must be a character vector of length 2.

