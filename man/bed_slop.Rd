% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bed_slop.r
\name{bed_slop}
\alias{bed_slop}
\title{increase the size of input intervals}
\usage{
bed_slop(bed_df, genome, both = 0, left = 0, right = 0, fraction = TRUE,
  strand = FALSE)
}
\value{
\code{data.frame}
}
\description{
increase the size of input intervals
}
\examples{
genome <- dplyr::tibble(
 ~chrom, ~size,
 "chr1", 5000
)

bed_df <- dplyr::tibble(
 ~chrom, ~start, ~end, ~name, ~score, ~strand,
 "chr1", 500,    1000, '.',   '.'     '+',
 "chr1", 1000,   1500, '.',   '.'     '-',
)

bed_df \%>\% bed_slop(left = 100)
bed_df \%>\% bed_slop(right = 100)
bed_df \%>\% bed_slop(both = 100)

bed_df \%>\% bed_slop(both = 0.5, fraction=TRUE)

}

